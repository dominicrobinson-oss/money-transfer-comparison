import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Best GBP to CAD Exchange Rate Today | Compare Money Transfer Providers",
  description: "Compare real-time GBP to CAD exchange rates from trusted money transfer providers. Send money to Canada with the best rates and lowest fees.",
  alternates: {
    canonical: "https://money-transfer-comparison.com/gbp-to-cad",
  },
};

export default function GbpToCadLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
