import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Best GBP to ZAR Exchange Rate Today | Compare Money Transfer Providers",
  description: "Compare real-time GBP to ZAR exchange rates from trusted money transfer providers. Send money to South Africa with the best rates and lowest fees.",
  alternates: {
    canonical: "https://money-transfer-comparison.com/gbp-to-zar",
  },
};

export default function GbpToZarLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
